{
  "name": "richcell",
  "version": "1.0.0",
  "description": "Excel 365 Add-in — WYSIWYG HTML editor that writes formatted HTML directly into cells",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && node generate-icons.js && npx office-addin-dev-certs install",
    "generate-icons": "node generate-icons.js",
    "generate-certs": "npx office-addin-dev-certs install",
    "validate": "npx office-addin-manifest validate manifest.xml"
  },
  "devDependencies": {
    "office-addin-dev-certs": "^1.9.0",
    "pngjs": "^7.0.0",
    "sharp": "^0.34.5"
  }
}
